Leitfaden FachgesprÃ¤ch: Modul 320 (OOP) mit JavaScriptUmfassende theoretische Vorbereitung & praktisches Mapping auf das Framework-Projekt Avenx-JS (Reines JavaScript-Umfeld)952500162879Fundamentale OOP-Konzepte: Klassen & ObjekteKlassen vs. Objekte (Instanzen) in JavaScriptEine Klasse beschreibt den Bauplan, wÃ¤hrend ein Objekt die konkrete AusprÃ¤gung mit eigenem Zustand im Arbeitsspeicher darstellt.Klassendefinition (HZ1 / HZ3): Definition von Datenstrukturen und Funktionen mittels des class-SchlÃ¼sselworts.Instanziierung: Erzeugung eines neuen Laufzeit-Objekts mittels new Klasse().KonzeptTheoretische Definition (JS)Praxisbezug im Framework "Avenx-JS"Klasse (Bauplan)class AvenxApp { ... } strukturiert den Einstiegspunkt der Anwendung und definiert die Kernlogik.Stellt die wiederverwendbare Architektur bereit, um isolierte App-Instanzen zu verwalten.Objekt (Instanz)const app = new AvenxApp({ el: '#app' }); erzeugt die konkrete Instanz imHeap-Speicher.Das konkrete Objekt verwaltet den spezifischenDOM-Knoten und steuert die laufende Applikation.Zustand und Verhalten (Felder & Methoden)Instanzvariablen: Properties, die im constructor Ã¼ber this.variableName oder als moderne Class Fields deklariert werden, um den Zustand zu halten.Methoden: Funktionen innerhalb der Klasse, die das Verhalten steuern und Berechnungen oder DOM-Interaktionen durchfÃ¼hren (z. B. AvenxCompiler.compile(template)).Objektinteraktion (HZ2): Spezialisierte Objekte kommunizieren miteinander, um komplexe Workflows zu realisieren (z. B. Ãœbergabe von geparsten Templates an den DomPatcher).Fortgeschrittene JavaScript OOP-Techniken & DatenkapselungKapselung & Zugriffskontrolle (Access Modifiers in JS)Im JavaScript-Umfeld wird Kapselung traditionell Ã¼ber Closures oder Namenskonventionen (z. B. Unterstrich _privateField) gelÃ¶st. Native private Felder werde mittels des #-PrÃ¤fixes geboten.Ã–ffentliche Schnittstelle (public): Methoden und Properties, auf die von aussen frei zugegriffen werden kann (z. B. die init()-Methode des Frameworks).Private Elemente (#private): Variablen oder Methoden, die strikt vor externer Manipulation geschÃ¼tzt sind und nur innerhalb der eigenen Klassengrenzen existieren.Das Geheimnisprinzip via JavaScript Proxies (Das HerzstÃ¼ck von Avenx-JS)WÃ¤hrend klassische OOP-Sprachen wie C# oder Java Properties (Getter/Setter) verwenden, nutzt unser Framework **JavaScript Proxies** (Ã¼ber die Klassen ProxyHandlerFactory und StateFactory) fÃ¼r eine hochdynamische Form der Kapselung und Zustandskontrolle.class StateFactory {static create(initialData) { return new Proxy(initialData, {set(target, key, value) { target[key] = value;}});domPatcher.patch(); return true;}}KonstruktorenDer constructor ist die fundamentale Initialisierungsmethode in JavaScript-Klassen. Er wird beim Aufruf von new automatisch ausgefÃ¼hrt und versetzt das Objekt in einen gÃ¼ltigen Startzustand (z. B. Initialisierung des reaktiven States in der Klasse AvenxComponent).Objektorientierte Beziehungen & ModularitÃ¤tVererbung (Prototypische)JavaScript nutzt die prototypische Vererbung. Das SchlÃ¼sselwort extends erlaubt es einer abgeleiteten Klasse, das Verhalten einer Basisklasse zu erben und zu erweitern. Mittels super() wird der Konstruktor der Elternklasse aufgerufen.Objektbeziehungen im Framework: Komposition vs. AggregationDas VerstÃ¤ndnis der Kopplung und Lebensdauer von Objekten ist essenziell fÃ¼r die Softwarearchitektur (HZ1):Komposition (Starke Bindung): Ein Objekt enthÃ¤lt andere Objekte, deren Lebensdauer strikt an den Container gebunden ist.Beispiel im Projekt: Eine Instanz von AvenxComponent erzeugt und besitzt zwingend einen eigenen internen ComponentParser. Stirbt die Komponente, wird auch der Parser verworfen.Aggregation (Schwache Bindung): Ein Objekt nutzt ein anderes Objekt, welches jedoch unabhÃ¤ngig existiert.Beispiel im Projekt: Die Klasse AvenxBridge verwaltet den globalen, shared Zustand. Einzelne Komponenten (AvenxComponent) abonnieren diesen Zustand (Aggregation), existieren aber unabhÃ¤ngig von der Bridge weiter.Analyse, Modellierung & QualitÃ¤tssicherungUML-Diagramme in JavaScript-Strukturen Ã¼bersetzen (HZ2 / HZ3)Klassendiagramme: Ãœbersetzung von UML-Klassenboxen in JavaScript-Dateien. Private Member (UML: -) werden als private Class Fields (#) oder Ã¼ber Module-Scoping umgesetzt; Ã¶ffentliche Member (UML: +) als regulÃ¤re Methoden.Sequenzdiagramme (HZ3): Modellierung des zeitlichen Ablaufs. In JavaScript entspricht dies der synchronen oder asynchronen Aufrufkette von Methoden Ã¼ber Modul- bzw. Klassengrenzen hinweg.SchrittBeteiligte Klasse / ModulAusgefÃ¼hrte Aktion / Methode1AvenxAppRuft beim Start LifecycleManager.init() auf.2LifecycleManagerInstanziiert den AvenxCompiler fÃ¼r das Template-Parsing.3AvenxCompilerNutzt den StyleProcessor, um CSS zu hashen (Scoped Styling).4ProxyHandlerFactoryRegistriert DatenÃ¤nderungen und veranlasst denSchrittBeteiligte Klasse / ModulAusgefÃ¼hrte Aktion / MethodeDomPatcher zur Aktualisierung.Dokumentation mit JSDoc (HZ3)Saubere Code-Dokumentation ist ein zentrales QualitÃ¤tskriterium. Durch den standardisierten Einsatz von JSDoc-Kommentaren direkt im JavaScript-Code wird die Lesbarkeit erhÃ¶ht und die automatische Generierung interaktiver EntwicklerhandbÃ¼cher ermÃ¶glicht (wie die bereitgestellte Dokumentationsseite des Projekts)./*** Kompiliert eine Avenx-Komponente und isoliert das Scoped CSS.* @param {string} template - Das rohe HTML/CSS-Template der Komponente.* @returns {Object} Das transformierte Komponenten-Objekt.*/compile(template) { ... }QualitÃ¤tssicherung via Unit Testing (HZ4)Modultests Ã¼berprÃ¼fen isolierte Funktionseinheiten auf Korrektheit. Auch im JavaScript-Umfeld (z. B. mit Testrunnern wie Jest, Vitest oder Mocha) folgt die Teststruktur strikt dem**AAA-Prinzip (Arrange, Act, Assert)**:Arrange: Vorbereiten der Testumgebung und Testdaten (Instanziierung der Hilfsklasse).Act: AusfÃ¼hren der spezifischen Zielmethode mit den Testdaten.Assert: ÃœberprÃ¼fung des Ergebnisses mittels Zusicherungen (Expectations).// Beispiel fÃ¼r einen JavaScript Unit Test der Framework-Hilfsklassen describe('Avenx-JS Hilfsklassen Tests', () => {test('HtmlEscaper sollte HTML-Tags maskieren', () => {// 1. ARRANGEconst rawInput = '<script>alert("xss")</script>';// 2. ACTconst escapedOutput = HtmlEscaper.escape(rawInput);});});// 3. ASSERTexpect(escapedOutput).not.toContain('<script>'); expect(escapedOutput).toContain('<script>');
